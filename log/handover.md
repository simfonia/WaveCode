# WaveCode 開發交接與進度

## 2026-03-08 ~ 2026-04-11 (略)

## 2026-04-12 (第一階段：錄音、靜音偵測、Reverb 與尾跡守衛機制)
(已記錄在原始檔案中)

## 2026-04-12 (第二階段：效能重組、樂句封裝與精密排程連動)
(已記錄在原始檔案中)

## 2026-04-13 (第一階段：樣板載入系統、IDE 互動修復與多採樣映射)
(已記錄在原始檔案中)

## 2026-04-13 (第二階段：取樣系統深度優化、兩層連動選單與全量範例遷移)

### 1. 本次對話達成
- **Web Audio 取樣系統優化 (Sampling System Optimization)**：
    - **[智慧分類]**：`AudioManager` 現能自動識別資料夾內容。含音名者歸類為「旋律類」(Melodic)，其餘歸類為「打擊類」(Percussion)。
    - **[ID 歧義消除]**：內部 ID 格式由 `_` 統一更換為 `::` (如 `jazzkit::kick`)。解決了檔名自帶底線導致的目錄解析錯誤。
    - **[效能監控]**：載入取樣時自動計算耗時與分類統計，並即時透過 `invoke` 輸出至 IDE 日誌面板。
- **Blockly 積木架構重整 (Blockly Architecture)**：
    - **[積木拆分]**：將單一取樣積木拆分為 `wc_sampler_percussion` (打擊類) 與 `wc_sampler_melodic` (旋律類)。
    - **[動態連動選單]**：打擊類積木實作了「資料夾 > 檔案」兩層連動下拉選單，選單內容隨磁碟實體檔案即時同步。
- **全量範例與文件遷移 (Examples & Docs)**：
    - **[範例更新]**：已手動更新 `ex_03` 到 `ex_08` 以及 `Serial_Interactions/` 下的所有官方範例，確保其在新架構下正常運作。
    - **[說明文件]**：重新編寫 `sampler.html` 與 `multisampler.html` (中英版)，詳細說明目錄結構與自動映射邏輯。
- **後端增強 (Rust Backend)**：
    - 更新 `lib.rs` 的 `SampleInfo` 結構，由後端直接提供精確的 `folder` 與 `filename` 欄位，減少前端計算開銷。

### 2. 下一步行動
- **UI 視覺化控制組件**：為效果器（Reverb/Delay）加入圖形化旋鈕或曲線編輯器。
- **取樣預聽功能**：在下拉選單中直接預覽選取的聲音。
- **MIDI 檔案匯入 (MIDI Import)**：支援將外部 MIDI 檔案轉換為 WaveCode 演奏序列。

==================================================
2026-04-13 (當前專案狀態)

1. 取樣架構：全面轉向「智慧分類 + 雙層連動選單」模式，極大提升了取樣選取的效率與精確度。
2. 系統穩定性：改用 `::` 分隔符號徹底根治了檔名歧義導致的 ID 遺失問題。
==================================================

## 2026-04-15 (MIDI 系統工業級升級 - #nyx 對齊)

### 1. 本次對話達成
- **MIDI 裝置通訊與 UI 同步**：
    - **[動態選單翻譯修復]**：徹底解決了積木從工具箱拉出後顯示 %BKY_...% 原始碼的問題。
    - **[API 字串回傳]**：在 api.js 的 getMidiOutputOptions 中直接解析 Blockly.Msg 回傳翻譯後的「所有裝置」，繞過 Blockly 的時序 Bug。
    - **[視覺化狀態回饋]**：MIDI 狀態圖示改用 CSS Filter (Hue-rotate) 實作，提升維護性並對齊 #nyx 綠色在線、粉色離線的設計。
- **MIDI 報導與轉換積木 (Reporter Blocks)**：
    - **[MIDI 轉頻率]**：實作 wc_midi_note_to_freq，支援將 MIDI Note ID 或音名 (String) 轉換為精確 Hz，解決低頻音色不準確的問題。
    - **[判定與工具]**：新增 wc_midi_note_to_name (轉音名) 與 wc_midi_is_pressed (全時鍵位判定)。
- **工具箱與分類**：
    - 重整 MIDI 分類，細分為「事件」、「工具」與「輸出」三大區塊，並配有輔助標籤與影音說明連結。

### 2. 下一步行動
- **MIDI 輸出壓力測試**：驗證在高頻傳送 CC 訊號時，對外部 DAW 或硬體的控制穩定性。
- **ADSR/Filter 實時對接**：開發 CC 訊號與樂器參數的直接映射工具 (CC-to-Param Mapping)。

==================================================
2026-04-15 (當前專案狀態)
- MIDI 系統現已具備跨裝置連動、自動掃描與高精度頻率轉換能力，技術架構與 HarmoNyx (#nyx) 完全同步。
==================================================
